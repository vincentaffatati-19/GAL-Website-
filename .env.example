# GAL public/runtime configuration template
# Never commit real secrets. Copy this file to a local .env file and supply values securely.

SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=

# Server-only values belong in the deployment platform secret store, never in client code.
# SUPABASE_SERVICE_ROLE_KEY=
